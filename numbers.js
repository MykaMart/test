// old
var a = 1;


// new

const a = 1;
// constants are immunitable, they cannot change (use them as the defualt, if they ned to cahnge make them lets later)

// let

let b = 2;
//l ets are mutable, they can change (a use case of this may be in loops, automatically deinfed as a var)



const multiple = 9 * 9;
const divide = 9 / 9;
const add = 9 + 9;
const subtraction = 4 - 4;
const modulus = 10 % 4;


// variable names should be
// camelCase
const randNumber = Math.random();

// try to make something that returns a number between 0 and 10
Math.floor(Math.random() * 11);
Math.trunc(Math.random() * 11);
Math.round(Math.random() * 10);

// typeof
// checks the data type
typeof (4);
typeof 8;

// whole number ex. 8 is an integer
// decimal ex.      8.11111 is a float
